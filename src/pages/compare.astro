---
import Layout from "../layouts/Layout.astro";
import Comparador from "../components/ia-hub/Comparador";
import { initialData } from "../lib/data";
---

<Layout title="Comparador IA - DirectorioIA">
    <Comparador client:load allData={initialData} />
</Layout>
